import { shallowMount } from '@vue/test-utils'
import btVueButton from '../bt-vue-button.vue'

describe('Component bt-vue-button default', () => {
    it('All data attributes are defined', () => {
        const wrapper = shallowMount(btVueButton)
        // console.log(wrapper)
    })
})